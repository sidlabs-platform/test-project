{
  "name": "demo-webapp",
  "version": "1.0.0",
  "description": "Demo web application with Azure DevOps pipelines",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --coverage",
    "lint": "eslint src/ tests/",
    "build": "echo 'Build step complete'"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^17.6.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
